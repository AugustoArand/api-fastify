services:
  - type: web
    name: api-harley-davidson
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm install && cd backend && npm install
    startCommand: node backend/src/server.js
    envVars:
      - key: NODE_VERSION
        value: 20.18.1
      - key: NODE_ENV
        value: production
